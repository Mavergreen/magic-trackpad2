# editor / compiler cruft
._*
*.o
*.d
spike_bind
.idea/
.ai/

# build outputs: unit tests -> build/tests/, pkg staging -> build/, the kext bundle
build/
# CMake out-of-tree build dir (Task 1+; kept separate from the Makefile's build/)
cmake-build/
cmake-build-debug/
# CMakePresets native/cross build dirs (mavericks-shipyard convention); still supported for a
# deliberate in-tree build via a local CMakeUserPresets.json override
build-native/
build-cross/
# per-developer preset override channel; never committed
CMakeUserPresets.json
kext-gesture/MavericksVoodooInputHost.kext/
kext-gesture/*.o

# root tool binaries (compiled from tools/*.c by `make`)
/mt2d
/multitouch_listen
/sbin/

# tools/ compiled helpers (the .c sources + the `re` toolkit are tracked; these built binaries aren't)
tools/feed_probe
tools/multitouch_contacts
tools/multitouch_cursorpos
tools/multitouch_transport
tools/synth_feed
tools/synth_tap
tools/click_monitor
tools/kc_lzss
tools/kc_carve
tools/macho_rebase
tools/multitouch_frames_probe
tools/mt2_name_probe

# transient working dirs
/docs/superpowers/
/reference/
.claude/settings.local.json
/tools/genuine-mt2-geometry.out
