- Nothing changes in how the trackpad works.

- Everything this installs is renamed to the project's new identity: the
  kext, the preference pane, the multitouch plugin, the background
  services and the installer package all move from `dev.modernmavericks.*`
  to `dev.mavergreen.*`. Installing this version retires the old ones, so
  nothing is left running twice.

- Restart after installing, as usual: the driver already in memory keeps
  running until you do.
